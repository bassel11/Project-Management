# API base URL for backend (change to your API gateway or service)
VITE_API_URL=https://api.your-domain.example

# Example: if you run backend locally on port 5000
# VITE_API_URL=http://localhost:5000
